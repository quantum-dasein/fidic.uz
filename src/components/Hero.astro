---
import { site } from '../data/site';
---
<section class="relative flex min-h-[92vh] items-center overflow-hidden">
  <!-- backdrop -->
  <div class="absolute inset-0 bp-grid"></div>
  <div class="pointer-events-none absolute -left-40 top-1/4 h-[40rem] w-[40rem] rounded-full opacity-[0.18] blur-[120px]" style="background: radial-gradient(circle, var(--color-brass), transparent 60%)"></div>
  <div class="pointer-events-none absolute -right-40 bottom-0 h-[36rem] w-[36rem] rounded-full opacity-[0.14] blur-[120px]" style="background: radial-gradient(circle, var(--color-blueprint), transparent 60%)"></div>

  <div class="relative mx-auto grid w-full max-w-7xl gap-14 px-5 py-24 md:px-8 lg:grid-cols-12 lg:items-center">
    <!-- Left -->
    <div class="lg:col-span-7">
      <div class="reveal chip">
        <span class="inline-block h-1.5 w-1.5 rounded-full bg-brass"></span>
        {site.tagline}
      </div>

      <h1 class="reveal mt-7 font-display text-[2.6rem] font-700 leading-[1.02] tracking-[-0.02em] text-fg sm:text-6xl lg:text-[4.6rem]" data-delay="80">
        Инфраструктурные<br />контракты, объяснённые<br />до последней <span class="text-brass-gradient">статьи</span>.
      </h1>

      <p class="reveal mt-7 max-w-xl text-lg leading-relaxed text-fg-dim" data-delay="160">
        FIDIC.uz — независимый центр знаний по контрактам FIDIC, EPC, claims, DAAB и проектам международных банков развития в Узбекистане и Центральной Азии.
      </p>

      <p class="reveal mt-5 font-mono text-sm text-muted" data-delay="200">
        Экспертиза в области:
        <span id="hero-typed" class="text-brass" data-words="FIDIC contracts|EPC / Turnkey|Claims & DAAB|Проектов МФО|Delay analysis|Procurement"></span><span class="inline-block w-[2px] animate-pulse bg-brass">&nbsp;</span>
      </p>

      <div class="reveal mt-9 flex flex-wrap items-center gap-4" data-delay="260">
        <a href="#suite" class="btn btn-primary">Открыть серию FIDIC
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none"><path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="#ask" class="btn btn-ghost">Спросить FIDIC AI</a>
      </div>
    </div>

    <!-- Right: stacked "contract card" -->
    <div class="reveal lg:col-span-5" data-delay="220">
      <div class="card relative p-6 md:p-8">
        <div class="flex items-center justify-between border-b border-line pb-4">
          <span class="font-mono text-xs uppercase tracking-widest text-muted">Conditions of Contract</span>
          <span class="flex gap-1.5">
            <span class="h-2.5 w-2.5 rounded-full" style="background: var(--color-fidic-red)"></span>
            <span class="h-2.5 w-2.5 rounded-full" style="background: var(--color-fidic-yellow)"></span>
            <span class="h-2.5 w-2.5 rounded-full" style="background: var(--color-fidic-silver)"></span>
          </span>
        </div>
        <div class="space-y-3 py-5 font-mono text-sm">
          <div class="flex items-baseline gap-3"><span class="text-brass">20.1</span><span class="text-fg-dim">Claims — единый порядок претензий обеих сторон</span></div>
          <div class="flex items-baseline gap-3"><span class="text-brass">20.2</span><span class="text-fg-dim">Уведомление <span class="text-fg">в течение 28 дней</span></span></div>
          <div class="flex items-baseline gap-3"><span class="text-brass">21.6</span><span class="text-fg-dim">DAAB → амикабельное урегулирование → арбитраж</span></div>
          <div class="h-px bg-line"></div>
          <div class="flex items-baseline gap-3"><span class="text-blueprint">8.5</span><span class="text-fg-dim">Extension of Time (EOT)</span></div>
          <div class="flex items-baseline gap-3"><span class="text-blueprint">13.3</span><span class="text-fg-dim">Variation procedure</span></div>
        </div>
        <div class="rounded-lg border border-brass/30 bg-brass/[0.06] p-4">
          <p class="text-sm leading-relaxed text-fg-dim">Не уверены, какая книга нужна вашему проекту? <a href="#suite" class="font-600 text-brass link-underline">Откройте радужную серию →</a></p>
        </div>
      </div>
    </div>
  </div>

  <!-- bottom stat strip -->
  <div class="absolute bottom-0 left-0 right-0 border-t border-line bg-ink/60 backdrop-blur-md">
    <div class="mx-auto grid max-w-7xl grid-cols-2 divide-x divide-line px-5 md:grid-cols-4 md:px-8">
      {[
        { n: '100+', l: 'стран применяют FIDIC' },
        { n: '9', l: 'книг радужной серии' },
        { n: '21', l: 'статья в изданиях 2017' },
        { n: 'МФО', l: 'World Bank · ADB · EBRD' },
      ].map((s) => (
        <div class="px-3 py-5 md:px-6">
          <p class="font-display text-2xl font-700 text-fg md:text-3xl">{s.n}</p>
          <p class="mt-0.5 text-xs text-muted">{s.l}</p>
        </div>
      ))}
    </div>
  </div>
</section>
